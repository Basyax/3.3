# 📝 Flask Blog / Post Management System

Учебный веб‑проект на **Flask** с системой регистрации и авторизации пользователей, созданием постов, комментариев и логированием действий (транзакций). Проект предназначен для демонстрации работы с Flask, SQLAlchemy, PostgreSQL и Flask‑Login.

---

## 🚀 Возможности проекта

- 🔐 **Регистрация и авторизация пользователей**
- 👤 Хранение данных пользователя (имя, фамилия, группа)
- 📝 **CRUD‑операции с постами** (создание, просмотр, редактирование, удаление)
- 💬 **Комментарии к постам** (только для авторизованных)
- 📜 **Логирование действий** (создание постов, комментариев, удаление)
- 🔒 Контроль прав доступа (редактировать и удалять может только автор)
- 🎨 HTML‑шаблоны + CSS

---

## 🛠 Используемые технологии

- **Python 3.x**
- **Flask**
- **Flask‑SQLAlchemy**
- **Flask‑Login**
- **Flask‑Bcrypt**
- **Flask‑WTF / WTForms**
- **PostgreSQL**
- **psycopg2**
- HTML / CSS

---

## 📂 Структура проекта

```
3.3/
│
├── app.py              # Основной файл приложения Flask (маршруты, модели, логика)
├── forms.py            # WTForms: формы постов, комментариев, регистрации и входа
├── instance/           # Конфигурации и локальные данные
├── static/             # CSS, изображения
├── templates/          # HTML-шаблоны Jinja2
│   ├── base.html       # Базовый шаблон (шапка, навигация, footer, flash-сообщения)
│   ├── index.html      # Главная страница
│   ├── posts.html      # Список всех постов
│   ├── post_detail.html# Детальный просмотр поста, комментарии, транзакции
│   ├── create_post.html# Создание и редактирование поста
│   ├── login.html      # Страница входа
│   ├── register.html   # Страница регистрации
│   ├── transactions.html # Журнал транзакций
│   └── layout.html     # Альтернативный layout (Bootstrap)
├── 3.3.rar             # Архив проекта
└── README.md           # Документация проекта
```

3.3/
│
├── app.py              # Основной файл приложения Flask (маршруты, модели, логика)
├── forms.py            # WTForms: формы постов, комментариев, регистрации и входа
├── instance/           # Конфигурации и локальные данные
├── static/             # CSS, изображения
├── templates/          # HTML‑шаблоны (Jinja2)
├── 3.3.rar             # Архив проекта
└── README.md           # Документация проекта
```

---

## ⚙️ Установка и запуск

### 1️⃣ Клонирование репозитория

```bash
git clone https://github.com/Basyax/3.3.git
cd 3.3
```

### 2️⃣ Создание виртуального окружения

```bash
python -m venv venv
source venv/bin/activate      # Linux / macOS
venv\Scripts\activate         # Windows
```

### 3️⃣ Установка зависимостей

```bash
pip install flask flask-sqlalchemy flask-login flask-bcrypt psycopg2-binary
```

### 4️⃣ Настройка базы данных PostgreSQL

Создайте базу данных и измените параметры подключения в `app.py`:

```python
DB_HOST = 'localhost'
DB_NAME = 'mydatabase'
DB_USER = 'postgres'
DB_PASSWORD = '1234'
```

⚠️ **Важно:** база данных должна существовать до запуска приложения.

---

### 5️⃣ Запуск приложения

```bash
python app.py
```

После запуска приложение будет доступно по адресу:

👉 **http://127.0.0.1:5000/**

---

## 👥 Пользовательские роли

- **Гость** — просмотр постов
- **Авторизованный пользователь**:
  - создание постов
  - редактирование и удаление своих постов
  - добавление комментариев

---

## 🧠 Логика безопасности

- Пароли хранятся в виде **bcrypt‑хэша**
- Используется **Flask‑Login** для управления сессиями
- Используется **Flask‑WTF** для защиты от CSRF‑атак
- Проверка уникальности `username` и `email` при регистрации
- Проверка прав доступа перед редактированием и удалением
- HTTP **403 Forbidden** при попытке несанкционированного доступа

---

## 📜 Транзакции (лог действий)

Все ключевые действия пользователей записываются в таблицу `Transaction`:

- `POST_CREATE` — создание поста
- `POST_UPDATE` — редактирование поста
- `POST_DELETE` — удаление поста
- `COMMENT_CREATE` — добавление комментария

Отдельная страница для просмотра истории действий:

```
/transactions
```

---

## 🖥 Интерфейс и шаблоны

Интерфейс приложения реализован с использованием **Jinja2** и наследования шаблонов.

### 🔹 base.html
- Общий шаблон для всех страниц
- Навигационное меню
- Отображение flash‑сообщений
- Поддержка авторизации (Login / Logout)

### 🔹 create_post.html
- Универсальная форма для **создания и редактирования постов**
- Используется `PostForm`

### 🔹 post_detail.html
- Детальная страница поста
- Отображение автора, даты публикации
- Комментарии
- Кнопки редактирования и удаления (только для автора)
- Просмотр транзакций, связанных с постом

### 🔹 login.html / register.html
- Формы входа и регистрации пользователей
- Защита CSRF
- Валидация данных

### 🔹 transactions.html
- Просмотр журнала действий пользователей

---

## 🎓 Назначение проекта

Проект создан в учебных целях и демонстрирует:

- архитектуру Flask‑приложения
- работу с ORM SQLAlchemy
- аутентификацию и авторизацию пользователей
- работу с формами Flask‑WTF
- шаблоны Jinja2 и наследование шаблонов
- базовые принципы MVC

Может использоваться как:
- курсовая / лабораторная работа
- пример Flask‑приложения с авторизацией
- основа для расширения функционала

---

## 📌 Автор

**Basyax**  
GitHub: https://github.com/Basyax

---

⭐ Если проект был полезен — поставь звезду репозиторию!

